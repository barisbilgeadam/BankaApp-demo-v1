package org.dorukt.utility;

/**
 * İlişkisel veya ilişkisel olmayan tüm database yapılarında kullanılmak üzere genişletilebilir
 * olan bu interface kullanılacaktır.
 *
 * @param <T> : Entity için tip belirtir. (Müşteri,Ürün vs gibi.)
 * @param <ID> : Entity içinde @Id ile işaretlenecek alanın tipi.(Integer,Long,Short vs.)
 *
 */
public interface IMyRepository<T,ID>{




}
